- type: gameMap
  id: ADT_Centcomm
  isGrid: true # Did you know that centcomm is the only "game map" that isn't actually a map? Send help.
  mapName: 'Станция ЦК'
  mapPath: /Maps/ADTMaps/ADTStations/adt_centcomm.yml
  minPlayers: 10
  stations:
    ADT_Centcomm:
      stationProto: NanotrasenCentralCommand
      components:
        - type: StationNameSetup
          mapNameTemplate: '{0} Станция ЦК {1}'
          nameGenerator:
            !type:NanotrasenNameGenerator
            prefixCreator: 'TG'
