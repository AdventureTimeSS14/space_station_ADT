chat-manager-send-mind-link-chat-wrap-message = [tfont="{ $fontType }" size={ $fontSize } defaultFont={ $defaultFont } defaultSize={ $defaultSize } ]{ $channel }: { $message }[/tfont]

chat-manager-send-mind-link-chat-wrap-message-name = [tfont="{ $fontType }" size={ $fontSize } defaultFont={ $defaultFont } defaultSize={ $defaultSize } ]{ $channel }: { $source }: { $message }[/tfont]
chat-manager-send-mind-link-chat-wrap-message-admin = [tfont="{ $fontType }" size={ $fontSize } defaultFont={ $defaultFont } defaultSize={ $defaultSize } ][color=#FF0000][bold](Админ)[/bold][/color] { $channel }: { $source }: { $message }[/tfont]
