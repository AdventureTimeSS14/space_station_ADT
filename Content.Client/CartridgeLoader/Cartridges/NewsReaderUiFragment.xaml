<cartridges:NewsReaderUiFragment xmlns:cartridges="clr-namespace:Content.Client.CartridgeLoader.Cartridges"
                               xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
                               xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                               xmlns:customControls="clr-namespace:Content.Client.Administration.UI.CustomControls"
                               xmlns="https://spacestation14.io"
                               Margin="1 0 2 0"
                               Orientation="Vertical"
                               HorizontalExpand="True"
                               VerticalExpand="True">
    <PanelContainer StyleClasses="BackgroundDark"></PanelContainer>
    <BoxContainer Orientation="Horizontal" HorizontalExpand="True" Margin="5,5,5,5">
        <Button
            Name="Prev"
            MinWidth="64"
            Disabled="True"
            HorizontalAlignment="Left"
            Text="{Loc 'news-read-ui-prev-text'}"
            ToolTip="{Loc 'news-read-ui-prev-tooltip'}"
            Access="Public"
            HorizontalExpand="True"/>
        <Button
            Name="Next"
            MinWidth="64"
            Disabled="True"
            HorizontalAlignment="Right"
            Text="{Loc 'news-read-ui-next-text'}"
            ToolTip="{Loc 'news-read-ui-next-tooltip'}"/>
    </BoxContainer>
    <controls:StripeBack Name="ArticleNameContainer">
        <PanelContainer>
            <Label Name="PageNum" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="4,0,0,0"/>
            <Label Name="PageName" Align="Center"/>
        </PanelContainer>
    </controls:StripeBack>

    <!-- Article body -->
    <PanelContainer Name="ArticlePane" VerticalExpand="True" HorizontalExpand="True">
        <PanelContainer.PanelOverride>
            <gfx:StyleBoxFlat BackgroundColor="#80808005" />
        </PanelContainer.PanelOverride>
        <ScrollContainer
                Name="PageTextScroll"
                HScrollEnabled="False"
                HorizontalExpand="True"
                MinSize="0 0"
                VerticalExpand="True">
            <BoxContainer
                Name="PageTextContainer"
                MinSize="0 0"
                Orientation="Vertical"
                VerticalExpand="True">
            </BoxContainer>
            <RichTextLabel Margin="8,8,8,8" Name="PageText" VerticalAlignment="Top"/>
        </ScrollContainer>
    </PanelContainer>

    <!-- Comments toggle -->
    <Button Name="CommentsButton"
            Text="{Loc news-read-ui-comments-button}"
            HorizontalExpand="True"
            Margin="5 4 5 0"
            StyleClasses="OpenBoth"
            Visible="False" />

    <!-- Draggable splitter between article and comments -->
    <cartridges:CommentsSplitterControl Name="CommentsSplitter"
                                        HorizontalExpand="True"
                                        Visible="False" />

    <!-- Comments section -->
    <PanelContainer Name="CommentsPanel"
                    VerticalExpand="True"
                    MinHeight="130"
                    Visible="False">
        <PanelContainer.PanelOverride>
            <gfx:StyleBoxFlat BackgroundColor="#80808008" />
        </PanelContainer.PanelOverride>
        <BoxContainer Orientation="Vertical" VerticalExpand="True" HorizontalExpand="True" Margin="5">
            <ScrollContainer Name="CommentsScroll"
                             HScrollEnabled="False"
                             VerticalExpand="True"
                             HorizontalExpand="True">
                <BoxContainer Name="CommentsList"
                              Orientation="Vertical"
                              HorizontalExpand="True"
                              VerticalExpand="True" />
            </ScrollContainer>
            <BoxContainer Orientation="Horizontal" HorizontalExpand="True" Margin="0 4 0 0">
                <LineEdit Name="CommentInput"
                          PlaceHolder="{Loc news-read-ui-comment-placeholder}"
                          HorizontalExpand="True"
                          StyleClasses="OpenRight" />
                <Button Name="CommentSendButton"
                        Text="{Loc news-read-ui-comment-send}"
                        MinWidth="80"
                        Disabled="True"
                        StyleClasses="OpenLeft" />
            </BoxContainer>
        </BoxContainer>
    </PanelContainer>

    <BoxContainer Orientation="Horizontal" HorizontalExpand="True" Margin="5,5,5,5">
        <Button
            Name="NotificationSwitch"
            ToolTip="{Loc news-reader-ui-mute-tooltip}"
            MinWidth="20"/>
        <RichTextLabel Margin="5,2,2,2" Name="ShareTime" VerticalAlignment="Top"/>
        <customControls:VSeparator Margin="2 0"/>
        <RichTextLabel Margin="5,2,2,2" Name="Author" VerticalAlignment="Top" HorizontalAlignment="Right"/>
    </BoxContainer>
</cartridges:NewsReaderUiFragment>
