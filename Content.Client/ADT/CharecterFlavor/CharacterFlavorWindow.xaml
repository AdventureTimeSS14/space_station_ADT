<controls:FancyWindow xmlns="https://spacestation14.io"
                      xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
                      xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                      xmlns:customControls="clr-namespace:Content.Client.Administration.UI.CustomControls"
                      xmlns:ui="clr-namespace:Robust.Client.UserInterface.Controls;assembly=Robust.Client"
                      Title="{Loc 'flavor-interface-lable'}"
                      MinSize="600 468"
                      SetSize="800 670">

        <BoxContainer Orientation="Vertical"
                      Margin="10"
                      HorizontalExpand="True"
                      VerticalExpand="True">

            <!-- Заголовок -->
            <BoxContainer Orientation="Horizontal" Margin="0 0 0 10">
                    <ui:RichTextLabel Name="LabelName"
                          Text="{Loc 'flavor-interface-lable'}"
                          HorizontalExpand="True"
                          Margin="8"/>
            </BoxContainer>

            <!-- ВЕРХНИЙ РЯД -->
            <BoxContainer Orientation="Horizontal"
                          HorizontalExpand="True"

                          SetHeight="300"
                          SeparationOverride="10">

                <!-- Headshot-->
                <PanelContainer VerticalExpand="True" SetWidth="275" Name="HeadshotContainer" >
                    <PanelContainer.PanelOverride>
                        <gfx:StyleBoxFlat BackgroundColor="#1a1a1a"/>
                    </PanelContainer.PanelOverride>

                    <BoxContainer Orientation="Vertical" Margin="8">
                        <ui:Label Text="{Loc 'flavor-interface-headshot'}"
                                  HorizontalAlignment="Center"
                                  StyleClasses="LabelHeading"
                                  Margin="5 0 5 5"/>

                        <TextureRect Name="HeadshotImage"
                                    SetSize="250 250"
                                    MaxSize="250 250"
                                    Stretch="KeepAspectCentered">
                            <RichTextLabel Name="HeadshotLoadingLabel"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                        </TextureRect>
                    </BoxContainer>
                </PanelContainer>

                <!-- FLAVOR TEXT -->
                <PanelContainer VerticalExpand="True" HorizontalExpand="True">
                    <PanelContainer.PanelOverride>
                        <gfx:StyleBoxFlat BackgroundColor="#1a1a1a"/>
                    </PanelContainer.PanelOverride>

                    <BoxContainer Orientation="Vertical" Margin="8">
                        <ui:Label Text="{Loc 'flavor-interface-flavor-text-title'}"
                                  StyleClasses="LabelHeading"
                                  Margin="8 0"/>

                        <ScrollContainer VerticalExpand="True"
                                         HorizontalExpand="True"
                                         VScrollEnabled="True"
                                         HScrollEnabled="False">
                            <ui:RichTextLabel Name="FlavorTextLabel"
                                              HorizontalExpand="True"
                                              VerticalAlignment="Top"
                                              HorizontalAlignment="Left"
                                              Margin="8"/>
                        </ScrollContainer>
                    </BoxContainer>
                </PanelContainer>
            </BoxContainer>

            <!-- НИЖНИЙ РЯД -->
            <BoxContainer Orientation="Horizontal"
                          HorizontalExpand="True"
                          VerticalExpand="True"
                          SeparationOverride="10"
                          Margin="0 10 0 0">

                <!-- CUSTOM SPECIES -->
                <PanelContainer HorizontalExpand="True" VerticalExpand="True" SetWidth="275" >
                    <PanelContainer.PanelOverride>
                        <gfx:StyleBoxFlat BackgroundColor="#1a1a1a"/>
                    </PanelContainer.PanelOverride>

                    <BoxContainer Orientation="Vertical" Margin="8" SetWidth="250">
                        <ui:Label Name="CustomSpeciesTitle"
                                  Text="{Loc 'flavor-interface-custom-species'}"
                                  StyleClasses="LabelHeading"
                                  Margin="8 0"/>

                        <ScrollContainer VerticalExpand="True"
                                         HorizontalExpand="True"
                                         VScrollEnabled="True"
                                         HScrollEnabled="False">
                            <ui:RichTextLabel Name="CustomSpeciesLabel"
                                              Text="{Loc 'flavor-interface-no-custom-species-desc'}"
                                              HorizontalExpand="True"
                                              VerticalAlignment="Top"
                                              HorizontalAlignment="Left"
                                              Margin="8"/>
                        </ScrollContainer>
                    </BoxContainer>
                </PanelContainer>

                <!-- OOC NOTES -->
                <PanelContainer HorizontalExpand="True" VerticalExpand="True" SizeFlagsStretchRatio="3" >
                    <PanelContainer.PanelOverride>
                        <gfx:StyleBoxFlat BackgroundColor="#1a1a1a"/>
                    </PanelContainer.PanelOverride>

                    <BoxContainer Orientation="Vertical" Margin="8">
                        <ui:Label Text="{Loc 'flavor-interface-OOC-notes'}"
                                  StyleClasses="LabelHeading"
                                  Margin="8 0"/>

                        <ScrollContainer VerticalExpand="True"
                                         HorizontalExpand="True"
                                         VScrollEnabled="True"
                                         HScrollEnabled="False">
                            <ui:RichTextLabel Name="OOCNotesLabel"
                                              HorizontalExpand="True"
                                              VerticalAlignment="Top"
                                              HorizontalAlignment="Left"
                                              Margin="8"/>
                        </ScrollContainer>
                    </BoxContainer>
                </PanelContainer>

            </BoxContainer>
        </BoxContainer>
</controls:FancyWindow>
