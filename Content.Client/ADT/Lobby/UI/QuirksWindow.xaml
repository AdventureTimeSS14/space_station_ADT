<controls:FancyWindow xmlns="https://spacestation14.io"
         xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
         xmlns:graphics="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
         SetSize="550 500"
         MinSize="500 128">
    <BoxContainer Name="MainContainer" Orientation="Vertical" VerticalExpand="True" Margin="5">
        <PanelContainer StyleClasses="BackgroundDark">
            <BoxContainer Name="PointsContainer" Orientation="Vertical">
                <Label Name="PointsLabel" Text="Lelelelele" Margin="5" HorizontalAlignment="Center"/>
                <RichTextLabel Name="SelectedQuirksLabel" Text="Lelelelele" Margin="5" HorizontalAlignment="Center"/>
            </BoxContainer>
        </PanelContainer>
        <ScrollContainer HorizontalExpand="True" VerticalExpand="True" HScrollEnabled="False" Margin="5">
            <PanelContainer VerticalExpand="True" HorizontalExpand="True">
                <PanelContainer.PanelOverride>
                    <graphics:StyleBoxFlat BackgroundColor="#1B1B1E" />
                </PanelContainer.PanelOverride>
                    <BoxContainer Name="QuirksContainer" VerticalExpand="True" HorizontalExpand="True" Orientation="Vertical">
                    </BoxContainer>
            </PanelContainer>
        </ScrollContainer>
    </BoxContainer>
</controls:FancyWindow>
