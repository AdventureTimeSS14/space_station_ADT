<controls:FancyWindow
    xmlns="https://spacestation14.io"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
    xmlns:cc="clr-namespace:Content.Client.Administration.UI.CustomControls"
    MinSize="924 450"
    SetSize="924 600"
    MaxWidth="924">
    <BoxContainer Name="MainContainer" Orientation="Vertical">
        <BoxContainer Orientation="Horizontal" Margin="8 0">
            <Label Name="CreditsLabel" HorizontalExpand="True" Margin ="4 4" />
            <Button
                Name="WithdrawButton"
                Text="{Loc 'store-ui-default-withdraw-text'}"
                Margin ="4 4"
                HorizontalAlignment="Right"/>
            <LineEdit Name="SearchBar" PlaceHolder="{Loc 'vending-machine-component-search-filter'}" HorizontalExpand="True" Margin ="4 4"/>
        </BoxContainer>
        <cc:HSeparator Margin="8 8 8 0" />

        <ScrollContainer VerticalExpand="True" Margin="4" HScrollEnabled="False">
                <GridContainer Name="VendingContents" HorizontalExpand="True" VerticalExpand="True" HorizontalAlignment="Center" Margin="4"/>
        </ScrollContainer>

         <!-- Footer -->
        <BoxContainer Orientation="Vertical">
            <PanelContainer StyleClasses="LowDivider" />
            <BoxContainer Orientation="Horizontal" Margin="10 2 5 0" VerticalAlignment="Bottom">
                <Label Text="{Loc 'vending-machine-flavor-left'}" StyleClasses="WindowFooterText" />
                <Label Text="{Loc 'vending-machine-flavor-right-new'}" StyleClasses="WindowFooterText"
                        HorizontalAlignment="Right" HorizontalExpand="True"  Margin="0 0 5 0" />
                <TextureRect StyleClasses="NTLogoDark" Stretch="KeepAspectCentered"
                        VerticalAlignment="Center" HorizontalAlignment="Right" SetSize="19 19"/>
            </BoxContainer>
        </BoxContainer>
    </BoxContainer>
</controls:FancyWindow>
