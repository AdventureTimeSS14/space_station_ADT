<BoxContainer
    xmlns="https://spacestation14.io"
    xmlns:graphics="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
    Orientation="Vertical"
    HorizontalExpand="True">

    <!-- Clickable chat row for DMs and joined groups -->
    <Button Name="ChatButton"
            StyleClasses="ButtonSquare"
            HorizontalExpand="True"
            MaxSize="137 64"
            Margin="0 1">
        <BoxContainer Orientation="Horizontal"
                      HorizontalExpand="True"
                      VerticalExpand="True"
                      MinWidth="132"
                      Margin="6 4"
                      VerticalAlignment="Center">
            <!-- Unread indicator dot -->
            <PanelContainer Name="UnreadIndicator"
                            MinSize="8 8"
                            MaxSize="8 8"
                            VerticalAlignment="Center"
                            Margin="0 0 6 0">
                <PanelContainer.PanelOverride>
                    <graphics:StyleBoxFlat
                        BackgroundColor="#17c622"
                        BorderColor="#0f7a15" />
                </PanelContainer.PanelOverride>
            </PanelContainer>

            <!-- Text container -->
            <BoxContainer Orientation="Vertical"
                          HorizontalExpand="True"
                          VerticalExpand="True"
                          VerticalAlignment="Center">
                <RichTextLabel Name="NameLabel"
                               StyleClasses="LabelHeading"
                               HorizontalExpand="True"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Margin="0 -2 0 0" />
                <Label Name="SubLabel"
                       StyleClasses="LabelSubText"
                       HorizontalExpand="True"
                       ClipText="False"
                       HorizontalAlignment="Center" />
            </BoxContainer>
        </BoxContainer>
    </Button>

    <!-- Info row for public groups and invites: name + action buttons stacked -->
    <PanelContainer Name="InfoPanel"
                    StyleClasses="ButtonSquare"
                    HorizontalExpand="True"
                    Margin="0 1"
                    Visible="False">
        <BoxContainer Orientation="Vertical"
                      HorizontalExpand="True"
                      Margin="6 4">
            <RichTextLabel Name="InfoNameLabel"
                           StyleClasses="LabelHeading"
                           HorizontalExpand="True"
                           HorizontalAlignment="Center"
                           Margin="0 -2 0 0" />
            <Label Name="InfoSubLabel"
                   StyleClasses="LabelSubText"
                   HorizontalExpand="True"
                   HorizontalAlignment="Left"
                   ClipText="True" />
            <BoxContainer Name="ActionButtons"
                          Orientation="Vertical"
                          HorizontalExpand="True"
                          Margin="0 3 0 0">
                <Button Name="JoinButton"
                        Text="{Loc nano-chat-group-join}"
                        HorizontalExpand="True"
                        MinHeight="28"
                        StyleClasses="OpenBoth" />
                <Button Name="AcceptButton"
                        Text="{Loc nano-chat-group-accept}"
                        HorizontalExpand="True"
                        MinHeight="28"
                        Margin="0 2 0 0"
                        StyleClasses="OpenBoth" />
                <Button Name="DeclineButton"
                        Text="{Loc nano-chat-group-decline}"
                        HorizontalExpand="True"
                        MinHeight="28"
                        Margin="0 2 0 0"
                        StyleClasses="OpenBoth" />
            </BoxContainer>
        </BoxContainer>
    </PanelContainer>
</BoxContainer>
