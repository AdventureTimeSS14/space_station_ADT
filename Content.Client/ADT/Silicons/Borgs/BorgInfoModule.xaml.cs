using Robust.Client.AutoGenerated;
using Robust.Client.UserInterface.Controls;
using Robust.Client.UserInterface.XAML;

namespace Content.Client.ADT.Silicons.Borgs;

[GenerateTypedNameReferences]
public sealed partial class BorgInfoModule : PanelContainer
{
    public BorgInfoModule(EntityUid entity, IEntityManager entityManager)
    {
        RobustXamlLoader.Load(this);

        ModuleView.SetEntity(entity);
        ModuleName.Text = entityManager.GetComponent<MetaDataComponent>(entity).EntityName;
    }
}
