<Control xmlns="https://spacestation14.io"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
         xmlns:parallax="clr-namespace:Content.Client.Parallax"
         xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls">

    <!-- Космос на фоне -->
    <parallax:ParallaxControl />

    <!-- Центрированное окно очереди -->
    <Control HorizontalAlignment="Center" VerticalAlignment="Center" MinSize="400 300">

        <!-- Фон окна с рамкой -->
        <PanelContainer StyleClasses="BackgroundDark" HorizontalExpand="True" VerticalExpand="True"/>

        <!-- Контент окна поверх фона -->
        <BoxContainer Orientation="Vertical" MinSize="200 200" SeparationOverride="8" Margin="12">

            <!-- Заголовок -->
            <BoxContainer Orientation="Horizontal">
                <Label Margin="8 0 0 0" Text="{Loc 'queue-title'}"
                       StyleClasses="LabelHeading" VAlign="Center" HorizontalExpand="True"/>
                <Button Name="QuitButton" Text="{Loc 'queue-quit'}" HorizontalExpand="True"/>
            </BoxContainer>

            <controls:HighDivider />

            <!-- Основная информация -->
            <BoxContainer Orientation="Vertical" VerticalExpand="True" Margin="0 20 0 0">
                <BoxContainer Orientation="Vertical">
                    <BoxContainer Orientation="Vertical" VerticalExpand="True">
                        <Label Text="{Loc 'queue-position'}" Align="Center"/>
                        <Label Name="QueuePosition" StyleClasses="LabelHeading" Align="Center"/>
                    </BoxContainer>
                    <BoxContainer Orientation="Vertical" VerticalExpand="True" Margin="0 5 0 0">
                        <Label Text="{Loc 'queue-total'}" Align="Center"/>
                        <Label Name="QueueTotal" StyleClasses="LabelHeading" Align="Center"/>
                    </BoxContainer>
                </BoxContainer>
            </BoxContainer>

            <!-- Кнопки -->
            <BoxContainer Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0 20 0 0">
                <Button Name="PriorityJoinButton" Text="{Loc 'queue-priority-join'}" HorizontalExpand="True" StyleClasses="OpenRight"/>
            </BoxContainer>
            <BoxContainer Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0 20 0 0">
                <Button Name="WebSiteButton" Text="{Loc 'queue-website'}" HorizontalExpand="True" StyleClasses="Caution"/>
            </BoxContainer>

        </BoxContainer>
    </Control>
</Control>
